version: 1
formatters:
  main:
    format: "%(levelname)s %(message)s : %(module)s - %(funcName)s"
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: main
    stream: ext://sys.stdout
root:
  level: NOTSET
  handlers: [console]
